package build.diplomacy

import mill._
import mill.scalalib._

object `package` extends build.ChippyModule {
  def publishVersion = "0.0.1"

  def moduleDeps = Seq(build.cde)

  def mvnDeps = Seq(
    mvn"org.chipsalliance::chisel:7.8.0",
  )
  def scalacPluginMvnDeps = Seq(
    mvn"org.chipsalliance:::chisel-plugin:7.8.0",
  )

  def pomSettings = makePomSettings("A parameter negotiation framework for Chisel.")
}
